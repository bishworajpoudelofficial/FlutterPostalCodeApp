class Postal {
  final String district;
  final String postOffice;
  final String postalCode;
  final String officeType;

  Postal(this.district, this.postOffice, this.postalCode, this.officeType);
}
